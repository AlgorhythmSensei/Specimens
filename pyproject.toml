[project]
name = "specimens"
version = "0.1.0"
description = "Browser-based evolutionary agent simulation engine"
requires-python = ">=3.9"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1"]

[tool.pytest.ini_options]
pythonpath = ["."]
